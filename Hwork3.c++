    #include <iostream>
    using namespace std;

    int main() {
        //* write a program that reads three integers -about the classROOM 
            int nb , ng , nt ;
            cin >> nb >> ng >> nt ;
            cout<< (nb > 25);
            cout<< (ng <= 30);
            cout << (nb>20 && nt > 2|| ng > 30 && nt >4);
            

        return 0;
    }